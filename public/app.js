const state={section:'vocab',level:'basic',mode:'all',search:'',questions:[],filtered:[],index:0,score:{}};
const sections={
  vocab:{title:'単語',description:'常用語から共通テスト・国公立二次・英検2級〜準1級・難関まで。意味だけでなく語法・例文・語族も確認。',levels:[['basic','🟢 基礎'],['standard','🔵 標準'],['developed','🟡 発展'],['advanced','🟠 上級'],['hard','🔴 難関']]},
  idiom:{title:'熟語',description:'重要熟語・群動詞・入試頻出表現。',levels:[['basic','基礎'],['standard','標準'],['entrance','入試'],['hard','発展']]},
  grammar:{title:'文法',description:'総文法の確認と文法クイズ。',levels:[['overview','3.1 総文法'],['quiz','3.2 文法クイズ']]},
  reading:{title:'長文',description:'長文コンテンツは準備中です。',levels:[['normal','通常長文'],['common','共通テスト'],['national','国公立二次'],['hard','難関大']]}
};
const $=id=>document.getElementById(id);
const progressKey='eng-god-progress';
const key=q=>`${q.type||state.section}:${q.id}`;
let manifests=null;
function loadProgress(){try{state.score=JSON.parse(localStorage.getItem(progressKey)||'{}')}catch{state.score={}}renderStats()}
function saveProgress(){localStorage.setItem(progressKey,JSON.stringify(state.score));renderStats()}
function renderStats(){const vals=Object.values(state.score);const answered=vals.reduce((n,v)=>n+(v.answered||0),0);const correct=vals.reduce((n,v)=>n+(v.correct||0),0);$('answeredCount').textContent=answered;$('correctCount').textContent=correct;$('accuracy').textContent=answered?`${Math.round(correct/answered*100)}%`:'0%'}
function renderNav(){const nav=$('categoryNav');nav.innerHTML='';for(const [id,s] of Object.entries(sections)){const h=document.createElement('div');h.className='nav-section';h.textContent=id==='vocab'?'① 単語':id==='idiom'?'② 熟語':id==='grammar'?'③ 文法':'⑤ 長文';nav.append(h);const b=document.createElement('button');b.className=`nav-item ${state.section===id?'active':''}`;b.textContent=s.title;b.onclick=async()=>{state.section=id;state.level=s.levels[0][0];state.search='';resetQuestionArea();renderAll();await fetchData()};nav.append(b)}}
function renderAll(){renderNav();const s=sections[state.section];$('pageTitle').textContent=s.title;$('pageDescription').textContent=s.description;const tabs=$('levelTabs');tabs.innerHTML='';s.levels.forEach(([id,label])=>{const b=document.createElement('button');b.className=`level-btn ${state.level===id?'active':''}`;b.textContent=label;b.onclick=async()=>{state.level=id;resetQuestionArea();renderAll();await fetchData()};tabs.append(b)});$('searchInput').value=state.search;$('modeSelect').value=state.mode;updateInfo()}
async function readManifest(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error(`${path} ${r.status}`);return r.json()}
async function bootManifests(){const [v,i,g]=await Promise.all([readManifest('data/vocab/index.json'),readManifest('data/idioms/index.json'),readManifest('data/grammar/index.json')]);manifests={vocab:v,idiom:i,grammar:g}}
function urlsFor(section,level){if(!manifests||section==='reading')return [];if(section==='vocab')return (manifests.vocab.levels[level]?.files||[]).map(f=>`data/vocab/${f}`);return (manifests[section][level]||[]).map(f=>`data/${section==='idiom'?'idioms':'grammar'}/${f}`)}
async function fetchData(){state.questions=[];updateInfo('読み込み中…');const urls=urlsFor(state.section,state.level);if(!urls.length){state.filtered=[];updateInfo(state.section==='reading'?'長文データは準備中です':'該当データなし');return}
  const settled=await Promise.all(urls.map(async u=>{try{const r=await fetch(u,{cache:'no-store'});if(!r.ok)return [];const d=await r.json();return Array.isArray(d)?d:[]}catch{return []}}));
  state.questions=settled.flat().filter(Boolean).map(normalizeQuestion);filterQuestions();
}
function normalizeQuestion(q){
  if(q.type==='vocab'||(!q.type&&q.word)) return {...q,type:'vocab'};
  if(Array.isArray(q.choices)) return q;
  return {...q,choices:[]};
}
function filterQuestions(){let qs=[...state.questions];const q=state.search.trim().toLowerCase();if(q)qs=qs.filter(x=>{const tags=Array.isArray(x.tags)?x.tags.join(' '):String(x.tags||'');return `${x.word||''} ${x.meaning||''} ${x.ja||''} ${x.question||''} ${x.prompt||''} ${tags}`.toLowerCase().includes(q)});if(state.mode==='weak')qs=qs.filter(x=>state.score[key(x)]?.wrong);if(state.mode==='unanswered')qs=qs.filter(x=>!(state.score[key(x)]?.answered));state.filtered=qs;updateInfo()}
function updateInfo(text){$('datasetInfo').textContent=text||`${sections[state.section].title} / ${state.level}：${state.filtered?.length||0}問`}
function resetQuestionArea(){$('questionArea').classList.add('hidden');$('questionArea').innerHTML='';$('emptyState').style.display='block';state.questions=[];state.filtered=[];updateInfo()}
function startQuiz(){if(!state.filtered?.length){$('emptyState').style.display='block';$('emptyState').querySelector('h2').textContent=state.section==='reading'?'長文データは準備中です':'該当する問題がありません';return}state.questions=[...state.filtered].sort(()=>Math.random()-.5);state.index=0;$('emptyState').style.display='none';showQuestion()}
function fillChoices(correct,candidates){const out=[correct];for(const c of candidates.filter(Boolean).sort(()=>Math.random()-.5)){if(!out.includes(c))out.push(c);if(out.length>=4)break}return out}
function vocabQuestion(q){const pool=state.questions.filter(x=>x.type==='vocab'&&x.word&&x.word!==q.word);const modes=[];if(q.meaning)modes.push('meaning');if(q.exampleSentence)modes.push('context');modes.push('jp');if(q.synonyms?.length)modes.push('synonym');if(q.family)modes.push('family');const mode=modes[Math.floor(Math.random()*modes.length)]||'meaning';if(mode==='meaning')return {mode,question:q.word,prompt:'最も適切な日本語の意味を選べ。',answer:q.meaning,choices:fillChoices(q.meaning,pool.map(x=>x.meaning))};if(mode==='jp')return {mode,question:q.meaning,prompt:'この意味に合う英単語を選べ。',answer:q.word,choices:fillChoices(q.word,pool.map(x=>x.word))};if(mode==='context'){const rx=new RegExp(q.word.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),'i');const sentence=rx.test(q.exampleSentence)?q.exampleSentence.replace(rx,'_____'):q.exampleSentence;return {mode,question:sentence,prompt:'文脈に最も適する語を選べ。',answer:q.word,choices:fillChoices(q.word,pool.map(x=>x.word)),extra:q.exampleJa||''};}if(mode==='synonym'){const a=q.synonyms[0];return {mode,question:q.word,prompt:`最も近い意味の語を選べ。（${q.meaning||''}）`,answer:a,choices:fillChoices(a,pool.flatMap(x=>x.synonyms||[]))};}const fam=String(q.family).split(';').map(s=>s.trim()).filter(Boolean);const a=fam[0]||q.word;return {mode,question:`「${q.word}」の語族・派生語として最も適切なものを選べ。`,prompt:'語彙のネットワークまで確認。',answer:a,choices:fillChoices(a,pool.flatMap(x=>String(x.family||'').split(';').map(s=>s.trim())))} }
function genericQuestion(q){let choices=[];if(Array.isArray(q.choices))choices=q.choices.map(c=>typeof c==='string'?c:c?.text).filter(Boolean);if(!choices.length&&q.answer)choices=[q.answer];return {mode:'standard',question:q.question||q.word||'',prompt:q.prompt||'',answer:q.answer||q.meaning||'',choices:fillChoices(q.answer||q.meaning||'',choices)}}
function makeQuestion(q){return q.type==='vocab'?vocabQuestion(q):genericQuestion(q)}
function showQuestion(){const q=state.questions[state.index];if(!q)return;const v=makeQuestion(q);const choices=[...v.choices].sort(()=>Math.random()-.5);const area=$('questionArea');area.classList.remove('hidden');area.innerHTML=`<div class="q-meta"><span class="tag">${escapeHtml(q.level||state.level)}</span><span>${state.index+1} / ${state.questions.length}</span><span class="tag">${escapeHtml(v.mode)}</span></div><div class="question">${escapeHtml(v.question)}<small>${escapeHtml(v.prompt||'')}</small></div><div class="answers" id="answers"></div><div id="explain"></div><div class="next-row"><button id="nextBtn" class="primary-btn" style="display:none">次の問題</button></div>`;const box=$('answers');choices.forEach(text=>{const b=document.createElement('button');b.className='answer-btn';b.textContent=text;b.onclick=()=>answer(q,text===v.answer,b,v);box.append(b)});$('nextBtn').onclick=()=>{state.index++;if(state.index>=state.questions.length){state.index=0;state.questions=[...state.filtered].sort(()=>Math.random()-.5);}$('explain').innerHTML='';showQuestion()};q._activeAnswer=v.answer}
function answer(q,isCorrect,btn,v){const k=key(q);const rec=state.score[k]||{answered:0,correct:0,wrong:0};rec.answered++;if(isCorrect){rec.correct++;btn.classList.add('correct')}else{rec.wrong++;btn.classList.add('wrong');document.querySelectorAll('.answer-btn').forEach(b=>{if(b.textContent===q._activeAnswer)b.classList.add('correct')})}state.score[k]=rec;document.querySelectorAll('.answer-btn').forEach(b=>b.disabled=true);let explanation=q.explanation||'この問題のポイントを確認しましょう。';if(q.type==='vocab'&&q.exampleSentence)explanation+=`\n例文：${q.exampleSentence}\n${q.exampleJa||''}`;$('explain').innerHTML=`<div class="explanation"><strong>${isCorrect?'正解！':'不正解'}</strong><br>${escapeHtml(explanation).replace(/\n/g,'<br>')}</div>`;$('nextBtn').style.display='inline-block';saveProgress()}
function escapeHtml(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
$('startBtn').onclick=startQuiz;$('shuffleBtn').onclick=async()=>{state.mode='all';state.search='';state.section='vocab';state.level='basic';resetQuestionArea();renderAll();await fetchData();startQuiz()};$('searchInput').oninput=e=>{state.search=e.target.value;filterQuestions()};$('modeSelect').onchange=e=>{state.mode=e.target.value;filterQuestions()};$('resetProgress').onclick=()=>{if(confirm('学習進捗をすべて削除します。よろしいですか？')){state.score={};saveProgress()}};
(async()=>{loadProgress();renderAll();try{await bootManifests();await fetchData()}catch(e){updateInfo('データの読み込みに失敗しました。JSON/配信先を確認してください。');console.error(e)}})();
